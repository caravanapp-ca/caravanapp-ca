import React from 'react';

import { SvgIconProps } from '@material-ui/core';
import { Flag } from '@material-ui/icons';

export default function EndIcon(props: SvgIconProps) {
  return <Flag {...props} />;
}
