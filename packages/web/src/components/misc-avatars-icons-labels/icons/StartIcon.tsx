import React from 'react';
import { OutlinedFlag } from '@material-ui/icons';
import { SvgIconProps } from '@material-ui/core/SvgIcon';

export default function StartIcon(props: SvgIconProps) {
  return <OutlinedFlag {...props} />;
}
