runtime: nodejs
env: flex

# https://cloud.google.com/appengine/docs/flexible/nodejs/configuring-your-app-with-app-yaml
# This sample incurs costs to run on the App Engine flexible environment.
# The settings below are to reduce costs during testing and are not appropriate
# for production use. For more information, see:
# https://cloud.google.com/appengine/docs/flexible/nodejs/configuring-your-app-with-app-yaml
manual_scaling:
  instances: 1

resources:
  cpu: .5
  memory_gb: 0.5
  disk_size_gb: 10

env_variables:
  COOKIE_SESSION_KEY: # GEnerate random characters, something like oawrcr48ont49n8u489unt4c9n8uoo
  DISCORD_BOT_SECRET: # Get from discord
  DISCORD_CLIENT_ID: # Get from discord
  DISCORD_CLIENT_SECRET: # Get from discord
  DISCORD_GUILD: # Get from discord
  DISCORD_GUILD_ID: # Get from discord
  DISCORD_REDIRECT: # https://{HOSTNAME}/api/auth/discord/callback'
  MONGO_DB: 'mongodb://${username}:${password}@35.226.113.101:27017/buddy_reading'

handlers:
  - url: /
    static_files: packages/web/build/index.html
    upload: packages/web/build/index.html
  - url: /
    static_dir: packages/web/build
# manual_scaling - Lock instances to 1 . using App Engine Flexible will create VM instances to serve from.
# If you want to lower your usage, you might restrict your App to just one 1 instance
# (probably not what you want in production, but perfect for in prototyping and development cases ).
# https://stackoverflow.com/questions/37381694/google-app-engine-how-to-correctly-deploy-an-app
